/**
 * Finance domain pack entry point.
 *
 * Phase-3 placeholder: this file will export a `DomainPack` describing the
 * hedge-fund agents, the ticker/portfolio start form, and the decision/signal
 * result view once those pieces are extracted out of the shared frontend
 * tree. Today it only documents the seam.
 */
export {};
