# For getting financial data to power the hedge fund
# Get your Financial Datasets API key from https://financialdatasets.ai/
FINANCIAL_DATASETS_API_KEY=your-financial-datasets-api-key

# For running LLMs hosted by openai (GPT 5, etc.)
# Get your OpenAI API key from https://platform.openai.com/
OPENAI_API_KEY=your-openai-api-key

# For running LLMs hosted by anthropic (claude-4-sonnet, claude-4.1-opus, etc.)
# Get your Anthropic API key from https://anthropic.com/
ANTHROPIC_API_KEY=your-anthropic-api-key

# Optional: route every Anthropic-shaped call (the Analyze stage's
# ChatAnthropic client AND the `claude` CLI used by the Patch stage)
# through an Anthropic-compatible provider — DeepSeek, Moonshot, a
# proxy, etc. Leave unset to hit api.anthropic.com directly.
#   ANTHROPIC_BASE_URL=https://api.deepseek.com/anthropic
# When this is set, ANTHROPIC_API_KEY should hold the provider's key
# (e.g. a DeepSeek key), not an Anthropic one.
# ANTHROPIC_BASE_URL=
#
# When ANTHROPIC_BASE_URL is set, the `claude` CLI uses Bearer auth via
# ANTHROPIC_AUTH_TOKEN, not the x-api-key flow that ANTHROPIC_API_KEY
# implies. Most relay providers (DeepSeek's /anthropic, OpenRouter, etc.)
# expect Bearer, so set both to the same value.
# ANTHROPIC_AUTH_TOKEN=your-provider-key
#
# Claude Code CLI references models by family alias (Sonnet / Opus /
# Haiku). When routing through an Anthropic-compatible provider, map
# each alias to that provider's actual model id so the Patch stage
# doesn't try to invoke a Claude model id that the proxy doesn't know.
# ANTHROPIC_DEFAULT_SONNET_MODEL=deepseek-v4-pro
# ANTHROPIC_DEFAULT_OPUS_MODEL=deepseek-v4-pro
# ANTHROPIC_DEFAULT_HAIKU_MODEL=deepseek-v4-pro

# For running LLMs hosted by deepseek (deepseek-chat, deepseek-reasoner, etc.)
# Get your DeepSeek API key from https://deepseek.com/
DEEPSEEK_API_KEY=your-deepseek-api-key

# For running LLMs hosted by groq (deepseek, llama3, etc.)
# Get your Groq API key from https://groq.com/
GROQ_API_KEY=your-groq-api-key

# For running LLMs hosted by gemini (gemini-2.5-flash, gemini-2.5-pro)
# Get your Google API key from https://ai.dev/
GOOGLE_API_KEY=your-google-api-key

# For running LLMs hosted by xAI (Grok 4, etc.)
XAI_API_KEY=your-xai-api-key

# For running LLMs hosted by Moonshot / Kimi (kimi-k2, kimi-latest, moonshot-v1-*)
# Get your Moonshot API key from https://platform.moonshot.ai/
# Mainland China users: set MOONSHOT_BASE_URL=https://api.moonshot.cn/v1
MOONSHOT_API_KEY=your-moonshot-api-key

# For running LLM GigaChat
GIGACHAT_API_KEY=your-gigachat-api-key

# For running LLMs hosted by OpenRouter
OPENROUTER_API_KEY=your-openrouter-api-key

# For running LLMs hosted by Microsoft Azure OpenAI (gpt-4o, gpt-4o-mini, etc.)
# Get your Azure OpenAI API settings from https://oai.azure.com/
AZURE_OPENAI_API_KEY=your-azure-openai-api-key
AZURE_OPENAI_ENDPOINT=your-azure-openai-endpoint
AZURE_OPENAI_DEPLOYMENT_NAME=your-azure-openai-deployment-name

# Jira MCP server used by the bug_fix domain
JIRA_BASE_URL=https://your-jira-host.example.com
JIRA_USERNAME=your-jira-username
JIRA_TOKEN=your-jira-personal-access-token

# Bitbucket MCP server used by the bug_fix Open PR stage
BITBUCKET_BASE_URL=https://your-bitbucket-host.example.com
BITBUCKET_USERNAME=your-bitbucket-username
BITBUCKET_TOKEN=your-bitbucket-personal-access-token

# Optional: override the path to the claude CLI used by the Patch stage.
# CLAUDE_CLI=/usr/local/bin/claude